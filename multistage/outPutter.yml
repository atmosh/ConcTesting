platform: linux
image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: taskRepo
outputs:
- name: firstOutput
- name: secondOutput

run:
  path: taskRepo/multistage/echoScriptMulti.sh
